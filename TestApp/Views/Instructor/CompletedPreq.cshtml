@model List<TestApp.Models.Learner>

<h1>Learners Who Have Completed Prerequisites</h1>

@if (Model != null && Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Learner ID</th>
                <th>First Name</th>
                <th>Last Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var learner in Model)
            {
                <tr>
                    <td>@learner.LearnerID</td>
                    <td>@learner.first_name</td>
                    <td>@learner.last_name</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No learners have completed the prerequisites for this course.</p>
}
