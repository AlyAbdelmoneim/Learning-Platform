@{
    ViewData["Title"] = "Quests";
}

<div class="container">
    <h2>Quests</h2>
    <div class="row">
        <div class="col-md-6 mb-3">
            <button onclick="window.location.href='@Url.Action("SkillMasteryQuests", "Instructor")'" class="btn btn-primary w-100">
                Skill Mastery Quests
            </button>
        </div>
        <div class="col-md-6 mb-3">
            <button onclick="window.location.href='@Url.Action("CollaborativeQuests", "Instructor")'" class="btn btn-primary w-100">
                Collaborative Quest
            </button>
        </div>
    </div>
</div>